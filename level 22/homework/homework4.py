def index_length(index_list):
    index_length_list = []
    for i in index_list:
        index_length_list.append(len(i))
    print(index_length_list) 
index_length(["nika", "vaja", "konstantine"])       